export interface IUser {
  _id: string;
  name: string;
  email: string;
  contactNumber: string;
  street: string;
  city: string;
  district: string;
  userProfile: string;
  userRole: string;
  uid: string;
}
